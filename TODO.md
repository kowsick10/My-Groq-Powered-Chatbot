# TODO

- [ ] Replace hard-coded Groq API key in `chatbot.py` with environment-variable based approach.
- [ ] Add `.gitignore` to avoid committing secrets (e.g., `.env`).
- [ ] Create/verify GitHub remote `origin` and push branch `main`.
- [ ] Provide final commands to user to complete pushing to GitHub.

